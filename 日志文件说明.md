# 日志文件说明

## 主要日志文件

### 1. `cleanup_cli_dubhe.log`
**用途：** cleanup_cli.php 的执行日志
**内容：**
- cleanup 执行状态
- 执行时间
- 是否成功执行
**示例：**
```
[2025-11-21 16:06:01] [xxx] update peer status ... done!
[2025-11-21 16:06:02] [xxx] calculate seeding bonus ... done!
[2025-11-21 16:06:02] [xxx] [CLEANUP_CLI], DONE: Cleanup ends at Priority Class 1, cost time in seconds: 1 ... done!
```

### 2. `nexus-cli-root-*.log` 和 `nexus-cli-www-data-*.log`
**用途：** CLI 命令的执行日志（通过 `do_log()` 函数记录）
**区别：**
- `nexus-cli-root-*.log`：以 root 用户执行的命令日志
- `nexus-cli-www-data-*.log`：以 www-data 用户执行的命令日志
**内容：**
- cleanup 执行的详细日志
- 任务分发日志
- SQL 执行日志
- 错误日志
**关键日志：**
- `CLEANUP_CLI_CALCULATE_SEED_BONUS_HANDLE_JOB`：魔力值计算任务执行日志
- `runBatchJobCalculateUserSeedBonus`：任务分发日志
- `SQL_SUCCESS` / `SQL_ERROR`：SQL 执行结果

### 3. `nexus-seed-bonus-points-cli-www-data-*.log`
**用途：** 魔力值计算的详细日志（每个用户的详细计算过程）
**内容：**
- 用户ID
- seed_points 旧值、增量、新值
- seedbonus 旧值、增量、新值
**格式：**
```
时间|用户ID|seed_points旧值|增量|新值|seedbonus旧值|增量|新值
2025-11-21 15:30:21|11259|2832.3|74.8|2907.1|2848.3|74.8|2923.1
```

### 4. `nexus-queue.log`
**用途：** 队列相关的日志（如果配置了队列日志）

### 5. `schedule_dubhe.log`
**用途：** Laravel schedule 的执行日志
**内容：**
- `php artisan schedule:run` 的执行结果
- 定时任务的执行状态

### 6. `nexus-update-cli-root-*.log`
**用途：** 系统更新相关的日志
**内容：**
- 代码更新过程
- 数据库迁移日志

### 7. `nexus-*.log`（不带 cli）
**用途：** Web 请求相关的日志（如果配置了）

## 关键日志查找

### 查找 cleanup 执行日志：
```bash
tail -20 /tmp/cleanup_cli_dubhe.log
```

### 查找魔力值计算任务日志：
```bash
grep -i "CLEANUP_CLI_CALCULATE_SEED_BONUS_HANDLE_JOB" /tmp/nexus-cli-root-*.log | tail -20
```

### 查找 SQL 执行日志：
```bash
grep -i "SQL_SUCCESS\|SQL_ERROR" /tmp/nexus-cli-root-*.log | tail -20
```

### 查找任务分发日志：
```bash
grep -i "runBatchJobCalculateUserSeedBonus\|batch_key:user_seed_bonus" /tmp/nexus-cli-root-*.log | tail -20
```

### 查看用户魔力值计算详情：
```bash
tail -20 /tmp/nexus-seed-bonus-points-cli-www-data-2025-11-21.log
```

## 日志文件大小管理

这些日志文件会不断增长，建议定期清理：

```bash
# 清理 7 天前的日志
find /tmp -name "nexus-*.log" -mtime +7 -delete
find /tmp -name "cleanup_cli_*.log" -mtime +7 -delete
```

## 日志级别

- `production.INFO`：普通信息日志
- `production.ERROR`：错误日志
- `production.WARNING`：警告日志
- `production.DEBUG`：调试日志（通常不记录）

