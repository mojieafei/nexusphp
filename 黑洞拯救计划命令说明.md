# 🕳️ 黑洞拯救计划 - 命令使用说明

## 📋 概述

黑洞拯救计划是一个自动化的系统，用于：
1. **追踪黑洞种子**：自动识别做种人数 <= 1 的种子（黑洞种子）
2. **自动促销**：为黑洞种子自动添加 2xfree 促销，直到脱离黑洞
3. **奖励机制**：对拯救做种人数=0且持续7天的种子的用户进行奖励

## 🎯 核心概念

### 黑洞种子定义
- **做种人数 <= 1** 的可见种子（不包括被封禁的种子）
- 这些种子正在被"黑洞"吞噬，需要用户拯救

### 奖励条件
- 种子做种人数 = 0
- 持续断种时间 >= 7天
- 用户对该种子：
  - 已完成下载（`snatched.finished = 'yes'`）
  - 正在做种（`peers.seeder = 'yes'`）
  - 做种时间 >= 7天（168小时）

## 📝 命令说明

### 基础命令

```bash
php artisan reward:zero-seeder-rescuers
```

### 可用参数

| 参数 | 说明 | 默认值 | 示例 |
|------|------|--------|------|
| `--days` | 做种人数=0持续天数（用于奖励判断） | 7 | `--days=10` |
| `--bonus` | 每个种子奖励的魔力值 | 1000 | `--bonus=2000` |
| `--min-seed-hours` | 用户对每个种子的最低做种时间要求（小时） | 168（7天） | `--min-seed-hours=240` |
| `--dry-run` | 仅预览，不实际发放奖励 | - | `--dry-run` |
| `--update-track` | 更新黑洞种子追踪表 | - | `--update-track` |
| `--only-update-track` | 只更新追踪表，不执行奖励流程 | - | `--only-update-track` |

## 🔧 使用场景

### 场景1：每天定时更新追踪表（推荐）

```bash
# 每天凌晨2点执行（crontab）
0 2 * * * cd /var/www/nexusphp && php artisan reward:zero-seeder-rescuers --only-update-track >> /dev/null 2>&1
```

**作用**：
- 扫描所有做种人数 <= 1 的种子
- 更新追踪表（`zero_seeder_torrents`）
- 为新加入黑洞的种子添加 2xfree 促销
- 为已脱离黑洞的种子移除促销

**输出示例**：
```
当前做种人数<=1的种子数量：120
更新 35 条记录，新增 8 条记录，删除 3 条记录（已恢复做种）
添加2xfree促销：8 个种子，移除促销：3 个种子
```

### 场景2：预览奖励结果

```bash
php artisan reward:zero-seeder-rescuers --dry-run
```

**作用**：
- 显示符合条件的种子数量
- 显示需要奖励的用户列表
- 显示预计发放的魔力值总数
- **不会实际发放奖励**

**输出示例**：
```
开始处理：做种人数=0且持续7天的种子奖励（黑洞页面显示做种人数<=1的种子）
每个种子奖励：1000魔力值
最低做种时间要求：168小时（7.0天）

=== 第二步：查找符合条件的种子 ===
找到 15 个符合条件的种子

=== 第三步：查找辅种用户 ===
找到 8 个用户，共辅种了 12 个种子
将发放总魔力值：12000

是否显示详细列表？ (yes/no) [yes]:
+----------+----------+----------+------------------+----------+
| 用户ID   | 用户名   | 辅种数量 | 平均做种时间      | 奖励魔力值 |
+----------+----------+----------+------------------+----------+
| 123      | user1    | 2        | 10.5天           | 2000     |
| 456      | user2    | 1        | 8.3天            | 1000     |
...
```

### 场景3：实际发放奖励

```bash
php artisan reward:zero-seeder-rescuers
```

**作用**：
- 执行完整的奖励流程
- 自动清理已恢复做种的种子记录
- 查找符合条件的种子和用户
- 发放魔力值奖励
- 标记种子为已奖励（`rewarded = 1`）

**交互提示**：
1. 是否显示详细列表？（默认：yes）
2. 确认发放奖励？（默认：no，需要手动确认）

### 场景4：更新追踪表并预览奖励

```bash
php artisan reward:zero-seeder-rescuers --update-track --dry-run
```

**作用**：
- 先更新追踪表
- 然后预览奖励结果
- 不实际发放奖励

### 场景5：自定义参数

```bash
# 断种10天以上才奖励，每个种子奖励2000魔力值，做种时间至少10天
php artisan reward:zero-seeder-rescuers --days=10 --bonus=2000 --min-seed-hours=240
```

## 📊 数据库表结构

### `zero_seeder_torrents` 表

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | mediumInteger | 自增主键 |
| `torrent_id` | mediumInteger | 种子ID（唯一） |
| `zero_seeder_start_time` | datetime | 做种人数变为<=1的开始时间 |
| `last_checked_at` | datetime | 最后检查时间 |
| `rewarded` | tinyInteger | 是否已奖励：0=未奖励，1=已奖励 |
| `created_at` | timestamp | 创建时间 |
| `updated_at` | timestamp | 更新时间 |

## ⚙️ 工作流程

### 1. 更新追踪表流程（`--only-update-track`）

```
扫描种子 (seeders <= 1)
    ↓
检查是否在追踪表中
    ↓
不在表中 → 创建记录 + 添加2xfree促销
    ↓
在表中：
  - seeders > 1 → 删除记录 + 移除促销
  - seeders <= 1 → 更新检查时间 + 确保有2xfree促销
```

### 2. 奖励发放流程（不带 `--only-update-track`）

```
清理已恢复的种子（seeders > 1）
    ↓
查找符合条件的种子（seeders = 0 且持续7天）
    ↓
查找符合条件的用户（已完成下载 + 正在做种 + 做种时间>=7天）
    ↓
显示预览（如果使用 --dry-run）
    ↓
确认后发放奖励（如果不是 --dry-run）
    ↓
标记种子为已奖励（rewarded = 1）
```

## 🎁 促销规则

### 自动添加2xfree促销

**触发条件**：
- 种子做种人数 <= 1
- 种子可见（`visible = 'yes'`）
- 种子未被封禁（`banned = 'no'`）

**促销设置**：
- `sp_state = 4`（2xfree：2倍上传且免费）
- `promotion_time_type = 1`（永久促销）
- `promotion_until = null`

### 自动移除促销

**触发条件**：
- 种子做种人数恢复 > 1

**操作**：
- `sp_state = 1`（normal）
- `promotion_time_type = 0`（跟随全局）
- `promotion_until = null`

## 📈 定时任务配置

### 推荐配置

```bash
# 编辑 crontab
crontab -u www-data -e

# 添加以下内容：
# 每天凌晨2点更新追踪表（只更新，不奖励）
0 2 * * * cd /var/www/nexusphp && /usr/bin/php artisan reward:zero-seeder-rescuers --only-update-track >> /var/log/nexusphp/blackhole_update.log 2>&1
```

**注意**：
- 将 `/var/www/nexusphp` 替换为实际项目路径
- 将 `/usr/bin/php` 替换为实际PHP路径（可用 `which php` 查看）
- 日志文件路径可根据需要调整

## 🔍 查看和验证

### 查看追踪表中的种子数量

```sql
-- 查看所有追踪记录
SELECT COUNT(*) FROM zero_seeder_torrents WHERE rewarded = 0;

-- 查看已恢复做种的种子（应该被自动清理）
SELECT z.torrent_id, t.name, t.seeders 
FROM zero_seeder_torrents z
JOIN torrents t ON z.torrent_id = t.id
WHERE t.seeders > 1 AND z.rewarded = 0;

-- 查看有2xfree促销的黑洞种子
SELECT t.id, t.name, t.seeders, t.sp_state
FROM torrents t
JOIN zero_seeder_torrents z ON t.id = z.torrent_id
WHERE t.seeders <= 1 
  AND t.sp_state = 4 
  AND z.rewarded = 0;
```

### 查看黑洞页面

访问：`https://你的域名/torrents.php?seeders_begin=0&seeders_end=1`

将显示所有做种人数 <= 1 的种子，并按做种人数升序排序。

## ⚠️ 注意事项

1. **奖励机制**：
   - 只有做种人数 = 0 且持续7天的种子才会被奖励
   - 黑洞页面显示的是做种人数 <= 1 的所有种子
   - 两者条件不同，请注意区分

2. **促销机制**：
   - 自动添加的2xfree促销是永久的
   - 只有当种子脱离黑洞（seeders > 1）时才会自动移除
   - 管理员手动修改促销状态不会影响自动化

3. **定时任务**：
   - 建议每天执行 `--only-update-track` 保持数据最新
   - 奖励发放建议每月手动执行一次，以便人工审核

4. **性能考虑**：
   - 更新追踪表的操作相对轻量，可以频繁执行
   - 奖励发放涉及大量数据查询，建议在低峰期执行

## 📝 常见问题

### Q: 为什么黑洞页面看不到种子？

A: 检查以下几点：
1. 是否有做种人数 <= 1 的可见种子
2. 是否排除了官方种子（tag_id=3）
3. 检查URL参数是否正确：`seeders_begin=0&seeders_end=1`

### Q: 为什么奖励命令找不到符合条件的种子？

A: 奖励条件是：
- `seeders = 0`（必须是0，不包括1）
- 持续断种时间 >= 7天
- `rewarded = 0`（未奖励过）
- 排除官方种子

### Q: 促销没有自动添加怎么办？

A: 确保：
1. 已执行 `--update-track` 或 `--only-update-track`
2. 种子确实满足条件（seeders <= 1, visible='yes', banned='no'）
3. 检查命令输出是否有错误信息

### Q: 如何手动为某个种子添加/移除促销？

A: 可以通过后台管理界面手动修改种子的促销状态。但注意：
- 如果种子仍在黑洞中（seeders <= 1），下次执行更新命令时会重新添加2xfree
- 如果种子已脱离黑洞（seeders > 1），不会自动添加促销

