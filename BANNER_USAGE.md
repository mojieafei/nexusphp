# Banner管理系统使用说明

## 🎯 功能概述

已成功将主页Banner从文件扫描方式升级为数据库管理，解决了大文件并发访问导致的服务器压力问题。

## ✨ 新增功能

### 1. **数据库管理**
- Banner信息存储在数据库中，支持灵活配置
- 支持图片和视频两种资源类型
- 支持相对路径和完整URL两种资源存储方式

### 2. **Filament后台管理**
- 访问路径：`/admin/system/banners`
- 只有管理员（class >= 13）可以访问

### 3. **权限控制**
- 支持按用户等级显示不同Banner（0-16等级）
- 游客、普通用户、VIP、管理员可见不同内容

### 4. **时间控制**
- 支持设置开始时间和结束时间
- 超出时间范围自动不显示

### 5. **宇航员主题Loading**
- 资源加载时显示宇航员漂浮动画
- 第一个资源加载完自动隐藏Loading
- 5秒超时自动隐藏（防止卡死）

### 6. **点击跳转**
- 可为每个Banner设置跳转链接
- 点击Banner自动跳转到指定页面

## 📝 使用步骤

### 步骤1：运行数据库迁移

```bash
php artisan migrate
```

这会创建 `banners` 表。

### 步骤2：访问管理页面

1. 登录管理员账号
2. 进入 Filament 后台
3. 找到 "System" -> "Banner管理"

### 步骤3：添加Banner

点击"Create"按钮，填写以下信息：

#### 基本信息
- **标题**：Banner的名称（必填）
- **是否启用**：开关控制是否显示
- **排序权重**：数字越大越靠前（默认0）

#### 资源设置
- **资源类型**：选择"图片"或"视频"
- **资源URL**：
  - 相对路径：`banner/aa.mp4`（旧方式兼容）
  - 完整URL：`https://cdn.example.com/banner.jpg`（推荐CDN）
- **跳转链接**：点击后跳转的URL（可选）

#### 显示设置
- **可见等级**：选择最低可见用户等级
  - 0 = 游客及以上可见
  - 10 = VIP及以上可见
  - 13 = 版主及以上可见
- **开始时间**：Banner开始显示时间（可选）
- **结束时间**：Banner结束显示时间（可选）

### 步骤4：迁移现有文件（可选）

如果您的 `public/banner/` 文件夹已有文件：

1. 保持文件在原位置
2. 在后台添加Banner记录，资源URL填写：`banner/文件名.扩展名`
3. 或者上传到CDN，填写完整URL

## 🔧 高级配置

### 用户等级对照表

```php
0  - 游客 (Peasant)
1  - 用户 (User)
2  - 高级用户 (Power User)
3  - 精英用户 (Elite User)
4  - 疯狂用户 (Crazy User)
5  - 疯狂进阶 (Insane User)
6  - 老兵 (Veteran User)
7  - 极限用户 (Extreme User)
8  - 终极用户 (Ultimate User)
9  - Nexus Master
10 - VIP
11 - 退休用户 (Retiree)
12 - 上传者 (Uploader)
13 - 版主 (Moderator)
14 - 管理员 (Administrator)
15 - 系统管理员 (SysOp)
16 - 站长 (Staff Leader)
```

### 资源优化建议

#### 1. 视频优化
```bash
# 使用FFmpeg压缩视频（推荐2-3M以内）
ffmpeg -i input.mp4 -c:v libx264 -crf 28 -preset slow -c:a aac -b:a 128k output.mp4
```

#### 2. 图片优化
- 使用WebP格式（体积小50%+）
- 宽度建议：1600px（匹配CONTENT_WIDTH）
- 压缩质量：80-85%

#### 3. CDN推荐
- 阿里云OSS
- 七牛云
- 腾讯云COS
- Cloudflare R2

## 🎨 自定义Loading样式

如需修改Loading效果，编辑 `include/functions.php` 中的 `display_homepage_banner()` 函数，找到：

```php
// 宇航员主题Loading效果
echo '<div class="banner-loading" style="...">';
```

修改样式或动画即可。

## 🐛 故障排查

### Banner不显示？

1. 检查数据库记录是否存在且`is_active = 1`
2. 检查时间范围是否在有效期内
3. 检查用户等级是否满足`visible_to`要求
4. 检查资源URL是否正确（路径/权限）

### Loading一直不消失？

- 检查资源URL是否可访问
- 检查浏览器控制台是否有错误
- 5秒后会自动隐藏

### 点击跳转不生效？

- 检查`jump_url`字段是否填写
- 检查URL格式是否正确

## 📊 性能对比

| 对比项 | 旧方案（文件扫描） | 新方案（数据库） |
|--------|-------------------|-----------------|
| 查询速度 | 文件系统IO | 数据库索引查询 ✅ |
| 权限控制 | 无 | 用户等级控制 ✅ |
| 时间控制 | 无 | 开始/结束时间 ✅ |
| 管理方式 | FTP上传 | 后台界面 ✅ |
| CDN支持 | 不支持 | 完整URL ✅ |
| 并发性能 | 文件锁竞争 | 缓存友好 ✅ |

## 🎉 完成！

现在您可以：
- ✅ 在后台灵活管理Banner
- ✅ 根据用户等级显示不同内容
- ✅ 设置展示时间范围
- ✅ 使用CDN加速资源加载
- ✅ 享受宇航员主题的Loading效果
- ✅ 支持点击跳转到指定页面

祝使用愉快！🚀

