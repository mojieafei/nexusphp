# 魔力值更新完整流程

## 第一步：用户做种汇报（实时）
**位置：** `public/announce.php` 第 654-656 行

```
用户做种 → announce.php → CleanupRepository::recordBatch($redis, $userid, $torrentid)
```

**作用：**
- 将用户ID和种子ID记录到 Redis 的 `batch_key:user_seed_bonus` 中
- 使用 Lua 脚本批量写入，避免并发问题
- **注意：** `batch_key:user_seed_bonus` 这个 key 是**永不过期**的（第 89 行判断）

## 第二步：Cleanup 定时执行（每小时）
**位置：** `include/cleanup.php` 第 346 行

```
crontab 每分钟执行 → cleanup_cli.php → docleanup() → runBatchJobCalculateUserSeedBonus()
```

**作用：**
- 从 Redis 的 `batch_key:user_seed_bonus` 中获取所有做种用户列表
- 过滤掉"死亡"用户（最后汇报时间 < deadtime）
- 将用户ID列表分批存入 `cleanup_batch_job_ids:*` Redis key（TTL 2小时）
- 分发 `CalculateUserSeedBonus` 任务到队列（带 delay）

## 第三步：队列任务执行（异步）
**位置：** `app/Jobs/CalculateUserSeedBonus.php`

```
Horizon 队列处理器 → CalculateUserSeedBonus::handle()
```

**执行流程：**
1. 从 `cleanup_batch_job_ids:*` Redis key 获取用户ID列表
2. 查询用户信息
3. 计算每个用户的魔力值增量
4. **执行 SQL 更新数据库**（第 186 行）

## 问题分析

### 问题1：Redis key 过期
**原因：**
- `cleanup_batch_job_ids:*` 的 TTL 是 1 小时（现在改为 2 小时）
- 如果任务延迟执行时间 + 执行时间 > TTL，key 就会过期
- 任务执行时无法获取用户ID列表，直接返回，不执行 SQL

**影响：**
- 队列中积压了很多任务，但都无法执行（key 过期）
- 数据库中的魔力值没有更新

### 问题2：batch_key:user_seed_bonus 可能不存在
**原因：**
- 如果用户11-18号更新后，没有新的做种汇报
- `batch_key:user_seed_bonus` 可能指向一个不存在的 hash key
- cleanup 执行时会报错："batch: xxx, not exists..."

**影响：**
- cleanup 无法获取用户列表，无法分发任务

### 问题3：队列积压
**原因：**
- 如果任务执行失败或 key 过期，任务会一直留在队列中
- 新的任务不断加入，但旧任务无法执行

**影响：**
- 队列中积压了很多任务，占用资源

## 解决方案

### 已修复：
1. ✅ 增加 Redis key TTL：从 1 小时改为 2 小时
2. ✅ 添加详细错误日志：当 key 过期时记录错误
3. ✅ 修复 cleanup_cli.php：直接执行 cleanup，跳过时间间隔检查

### 待处理：
1. ⚠️ 清理队列中积压的任务（key 已过期的任务）
2. ⚠️ 检查 batch_key:user_seed_bonus 是否存在
3. ⚠️ 如果 batch 不存在，需要重新生成

## 建议操作

1. **清理队列中积压的任务：**
```bash
# 清空队列（谨慎操作）
php artisan queue:flush
```

2. **检查 batch_key:user_seed_bonus：**
```bash
php check_redis_keys.php
```

3. **等待下一次 cleanup 执行：**
- cleanup 会重新从 batch_key:user_seed_bonus 获取用户列表
- 重新分发任务到队列
- 新的任务使用 2 小时 TTL，不会过期

4. **监控任务执行：**
```bash
# 检查任务是否成功执行
grep -i "CLEANUP_CLI_CALCULATE_SEED_BONUS_HANDLE_JOB.*SQL_SUCCESS\|SQL_ERROR" /tmp/nexus-cli-root-*.log | tail -20
```

