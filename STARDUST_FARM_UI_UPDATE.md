# 🎨 星尘农场 - UI更新说明

## ✅ 已添加功能

### 1. 购买土地按钮

**位置**：农场信息区域下方（仅在自己的农场显示）

**显示逻辑**：
```
如果已有12块地 → 隐藏按钮
如果下一块地免费 → 隐藏按钮（自动获得）
如果等级不足 → 🔒 需要 Veteran User 等级（灰色，不可点击）
如果星尘不足 → 💰 星尘不足（需要 500⭐）（灰色，不可点击）
如果等级和星尘都够 → 🏞️ 购买土地（500⭐）（绿色，可点击）
```

**提示信息**：
- 等级不足：`⚠️ 购买土地需要达到 Veteran User 等级`
- 星尘不足：`还需 300⭐ 星尘`
- 可购买：`✓ 可购买第 4 块土地`

---

## 🎮 用户交互流程

### 购买土地流程

1. **进入自己的农场**
   - 访问 `https://dubhe.site/stardust_farm.php`

2. **查看购买按钮状态**
   - 位置：农场信息区域（星尘/等级/经验/土地）下方
   - 实时显示当前状态和价格

3. **点击购买按钮**
   - 弹出确认框：`确定花费 500⭐ 星尘购买第 4 块土地吗？`
   - 点击"确定"后自动购买

4. **购买成功**
   - 显示成功通知：`✓ 成功购买土地`
   - 自动刷新农场数据
   - 新土地立即出现在农场中

---

## 💡 UI状态示例

### 情况1：等级不足
```
┌─────────────────────────────────────┐
│  🏞️ 购买土地按钮                    │
│  ┌───────────────────────────────┐  │
│  │ 🔒 需要 Veteran User 等级     │  │
│  └───────────────────────────────┘  │
│  ⚠️ 购买土地需要达到 Veteran User  │
│     等级                            │
└─────────────────────────────────────┘
```

### 情况2：星尘不足
```
┌─────────────────────────────────────┐
│  🏞️ 购买土地按钮                    │
│  ┌───────────────────────────────┐  │
│  │ 💰 星尘不足（需要 500⭐）     │  │
│  └───────────────────────────────┘  │
│  还需 300⭐ 星尘                    │
└─────────────────────────────────────┘
```

### 情况3：可以购买
```
┌─────────────────────────────────────┐
│  🏞️ 购买土地按钮                    │
│  ┌───────────────────────────────┐  │
│  │ 🏞️ 购买土地（500⭐）          │  │ ← 可点击
│  └───────────────────────────────┘  │
│  ✓ 可购买第 4 块土地                │
└─────────────────────────────────────┘
```

---

## 🔧 技术实现

### 前端代码位置
- **HTML结构**：`public/stardust_farm.php` 第607-616行
- **JS渲染逻辑**：`updatePurchaseLandButton()` 函数
- **JS购买逻辑**：`purchaseLand()` 函数

### 数据流
```
1. 页面加载 → loadFarmData()
2. 获取农场数据 → 包含 can_purchase_land, next_land_price, required_class_name
3. 渲染农场 → renderFarm()
4. 更新按钮 → updatePurchaseLandButton()
5. 用户点击 → purchaseLand()
6. 调用API → ajax.php?action=purchaseStardustLand
7. 购买成功 → 刷新数据 → loadFarmData()
```

---

## 📱 响应式设计

按钮会自动适配：
- 宽度：100%（占满容器）
- 字体大小：16px（易读）
- 提示信息：12px（不抢眼）
- 颜色：紫色渐变（醒目但不刺眼）

---

## 🎊 测试建议

### 测试场景

1. **新用户（等级低于Veteran User）**
   - ✓ 按钮显示"🔒 需要 Veteran User 等级"
   - ✓ 按钮不可点击（灰色）
   - ✓ 显示等级要求提示

2. **Veteran User（星尘不足）**
   - ✓ 按钮显示"💰 星尘不足（需要 500⭐）"
   - ✓ 按钮不可点击（半透明）
   - ✓ 显示还需多少星尘

3. **Veteran User（星尘充足）**
   - ✓ 按钮显示"🏞️ 购买土地（500⭐）"
   - ✓ 按钮可点击（鲜艳颜色）
   - ✓ 点击后弹出确认框
   - ✓ 确认后成功购买
   - ✓ 自动刷新并显示新土地

4. **已有12块地**
   - ✓ 按钮完全隐藏

---

## 🚀 部署完成

所有功能已实现并立即生效：
- ✅ 购买土地UI
- ✅ 实时状态更新
- ✅ 等级和星尘检查
- ✅ 动态价格显示
- ✅ 确认对话框
- ✅ 成功提示

**刷新农场页面即可看到购买土地按钮！** 🎉

